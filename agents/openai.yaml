interface:
  display_name: "Skill Sync"
  short_description: "Scan and safely share skills across local AI agents"
  default_prompt: "Use $skill-sync to discover local skills across Codex, Claude, OpenClaw, OpenCode, and shared libraries, then report shared vs platform-specific skills."
