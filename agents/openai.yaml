interface:
  display_name: "Skill Sync"
  short_description: "One source of truth for local AI agent skills"
  default_prompt: "Use $skill-sync to audit skills across Codex, Claude, OpenClaw, OpenCode, workspace skills, and shared roots, then score hygiene, show recommended actions, diff conflicting installs, and produce or apply a safe canonical-source convergence and migration plan."
