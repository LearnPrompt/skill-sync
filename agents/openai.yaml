interface:
  display_name: "Skill Sync"
  short_description: "Audit, diff, and converge skills across local AI agents"
  default_prompt: "Use $skill-sync to audit local skills across Codex, Claude, OpenClaw, OpenCode, workspace skills, and shared roots, then score hygiene, show recommended actions, and propose a safe canonical-source convergence plan."
